@using MoeKinoWebApp.Models;

@{
    Layout = "_Layout";
}

@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@model IEnumerable<MoviePaticipantCategory>

@{
    ViewData["Title"] = "Index";
}


<div class="container pt-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">MoviePaticipantCategories List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="MoviePaticipantCategory", asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Create New MoviePaticipantCategory
            </a>
        </div>
    </div>
</div>
<br>

<table class="table table-bordred table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Id
            </th>
            <th>
                Name En
            </th>
            <th>
                Name Ru
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
                <td width="30%">
                    @obj.Id
                </td>
                <td width="30%">
                    @obj.NameEn
                </td>
                <td width="30%">
                    @obj.NameRu
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="MoviePaticipantCategory" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2">
                            <i class="bi bi-pencil-square"></i> Edit
                        </a>
                        <a asp-controller="MoviePaticipantCategory" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                            <i class="bi bi-trash-fill"></i> Delete
                        </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
